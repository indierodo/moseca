services:
  moseca:
    build: .
    ports:
      - 7860:7860
    #uncomment if you need GPU support:
    #deploy:
      #resources:
        #reservations:
          #devices:
            #- driver: nvidia
              #count: 1
              #capabilities: [gpu]
